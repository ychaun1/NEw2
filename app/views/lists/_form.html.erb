<%= form_for @list, :remote => true do |f| %>
  <div class="input-group">
  <%= f.text_area :title, :class => "form-control" %>
  <div class="input-group-addon">
      <button type="submit" class="my-btn btn-info glyphicon glyphicon-floppy-disk">
      </button>
  </div>
</div>
<% end %>
